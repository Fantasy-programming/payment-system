{
  "name": "@mikcloud/ros-server",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "cross-env NODE_ENV=production bun run dist/index.js",
    "dev": "cross-env NODE_ENV=development bun run --watch src/index.ts",
    "test": "cross-env NODE_ENV=test bun test",
    "lint": "eslint --ext .ts .",
    "build": "cross-env NODE_ENV=production bun build --entrypoint src/index.ts --minify --outdir ./dist --target=bun"
  },
  "dependencies": {
    "@mikcloud/routeros": "workspace:*",
    "@mikcloud/logger": "workspace:*",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "express": "^4.19.2",
    "express-async-errors": "^3.1.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.23.8"
  }
}
